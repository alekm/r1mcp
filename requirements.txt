# MCP 2.x — the 2026-07-28 spec. The SDK reorganized in 2.0: `mcp.server.fastmcp.FastMCP`
# became `mcp.server.mcpserver.MCPServer`, so 1.x will not import this server.
mcp[cli]>=2.0.0,<3
httpx>=0.27
python-dotenv>=1.0
